export class WebConfig {
    chatDB: string;
    fileDB: string;
    port: number;
    timeout: number;
    webserver: string;

    pushServer: string;
    ParseApplicationId: string;
    ParseRESTAPIKey: string;
    ParseMasterKey: string;
}